const { parseUrl } = require("./parseUrl");
const {
  SEPARATOR,
  ENDPOINTS,
  REQKEYS,
  SEARCH_ENDPOINT,
  CRUD,
  HEADERS,
  HELLO_MESSAGE,
} = require("./constants");

module.exports = {
  parseUrl,
  SEPARATOR,
  ENDPOINTS,
  REQKEYS,
  SEARCH_ENDPOINT,
  CRUD,
  HEADERS,
  HELLO_MESSAGE,
};
